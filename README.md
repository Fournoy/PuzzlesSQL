# PuzzlesSQL
PuzzlesSQL is a Python package designed to simplify and accelerate SQL Injection (SQLi) automation. It provides pre-built functions that users can combine to create custom scripts. Currently, it supports time-based blind SQLi, with plans to expand to other types of SQLi in the future.
